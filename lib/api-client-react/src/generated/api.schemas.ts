/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Learning Companion Bot API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface AiRequest {
  /** The user's question or message */
  message: string;
  /** Optional session ID for conversation continuity */
  sessionId?: string;
  /** Optional topic context */
  topic?: string;
  /** When true, the message is not persisted to the database */
  temporary?: boolean;
}

/**
 * Which key was used for the most recent request
 */
export type AiUsageStatsActiveKey =
  (typeof AiUsageStatsActiveKey)[keyof typeof AiUsageStatsActiveKey];

export const AiUsageStatsActiveKey = {
  primary: "primary",
  backup: "backup",
  none: "none",
} as const;

export interface AiUsageStats {
  /** Tokens consumed by the primary API key today */
  primaryTokensUsed: number;
  /** Tokens consumed by the backup API key today */
  backupTokensUsed: number;
  /** Per-key daily token limit */
  dailyLimit: number;
  /** Which key was used for the most recent request */
  activeKey: AiUsageStatsActiveKey;
  /** Whether the primary key is currently rate-limited */
  primaryExhausted: boolean;
  /** Whether the backup key is currently rate-limited */
  backupExhausted: boolean;
  /** ISO timestamp when the primary key cooldown lifts (null if available) */
  primaryRetryAfter: string | null;
  /** ISO timestamp when the backup key cooldown lifts (null if available) */
  backupRetryAfter: string | null;
  /** Whether a backup API key is configured */
  hasBackupKey: boolean;
  /** ISO timestamp when the counters next reset (midnight UTC) */
  resetAtUtc: string;
}

export interface AiResponse {
  /** The AI assistant's response */
  reply: string;
  /** Session ID for this conversation */
  sessionId: string;
  timestamp: string;
}

export type ChatMessageRole =
  (typeof ChatMessageRole)[keyof typeof ChatMessageRole];

export const ChatMessageRole = {
  user: "user",
  assistant: "assistant",
} as const;

export interface ChatMessage {
  id: string;
  role: ChatMessageRole;
  content: string;
  timestamp: string;
}

export interface ConversationHistory {
  sessionId: string;
  messages: ChatMessage[];
}

export type TopicDifficulty =
  (typeof TopicDifficulty)[keyof typeof TopicDifficulty];

export const TopicDifficulty = {
  beginner: "beginner",
  intermediate: "intermediate",
  advanced: "advanced",
} as const;

export interface Topic {
  id: string;
  title: string;
  description: string;
  category: string;
  difficulty: TopicDifficulty;
}

export interface SuggestedTopics {
  topics: Topic[];
}

export interface ProjectStep {
  id: number;
  goal: string;
}

export interface Project {
  title: string;
  steps: ProjectStep[];
}

export interface LessonRequest {
  stepId: number;
}

export interface LessonResponse {
  explanation: string;
  task: string;
  hint: string;
}

export interface LearnSuggestion {
  id: string;
  title: string;
  description: string;
}

export interface LearnSuggestions {
  suggestions: LearnSuggestion[];
}

export type LearnLessonRequestDifficulty =
  (typeof LearnLessonRequestDifficulty)[keyof typeof LearnLessonRequestDifficulty];

export const LearnLessonRequestDifficulty = {
  beginner: "beginner",
  intermediate: "intermediate",
  advanced: "advanced",
} as const;

export interface LearnLessonRequest {
  language: string;
  difficulty: LearnLessonRequestDifficulty;
  topic: string;
  /**
   * @minimum 1
   * @maximum 5
   */
  level: number;
}

export interface LearnLessonResponse {
  level: number;
  totalLevels: number;
  levelTitle: string;
  explanation: string;
  codeExample: string;
  task: string;
  hint: string;
}

export type SaveLearnProgressRequestDifficulty =
  (typeof SaveLearnProgressRequestDifficulty)[keyof typeof SaveLearnProgressRequestDifficulty];

export const SaveLearnProgressRequestDifficulty = {
  beginner: "beginner",
  intermediate: "intermediate",
  advanced: "advanced",
} as const;

export interface SaveLearnProgressRequest {
  userId: string;
  language: string;
  difficulty: SaveLearnProgressRequestDifficulty;
  topic: string;
  completedLevels: number[];
}

export interface LearnProgressItem {
  id: string;
  userId: string;
  language: string;
  difficulty: string;
  topic: string;
  completedLevels: number[];
  updatedAt: string;
}

export interface LearnStreak {
  /** Consecutive days with at least one lesson completed, ending today or yesterday */
  currentStreak: number;
  /** The all-time longest streak in days */
  longestStreak: number;
  /** Whether the user has completed a lesson today */
  todayActive: boolean;
  /** ISO date string of the most recent activity (YYYY-MM-DD) */
  lastActivityDate: string | null;
}

export interface LearnActivityDates {
  /** Array of YYYY-MM-DD strings on which the user had activity */
  dates: string[];
  /** The earliest date included in the range (YYYY-MM-DD) */
  startDate: string;
  /** The latest date included in the range (YYYY-MM-DD) */
  endDate: string;
}

export interface LearnProgressList {
  items: LearnProgressItem[];
}

export interface DeleteResponse {
  success: boolean;
  sessionId: string;
}

export interface CertificateRecord {
  id: string;
  userId: string;
  userName: string;
  language: string;
  topic: string;
  issuedAt: string;
}

export interface CertificateList {
  items: CertificateRecord[];
}

export interface CreateCertificateRequest {
  userId: string;
  userName?: string;
  language: string;
  topic: string;
}

export interface ErrorResponse {
  error: string;
  message: string;
}

export type GetConversationHistoryParams = {
  sessionId?: string;
};

export type ListCertificatesParams = {
  userId: string;
};

export type GetLearnSuggestionsParams = {
  language: string;
  difficulty: GetLearnSuggestionsDifficulty;
};

export type GetLearnSuggestionsDifficulty =
  (typeof GetLearnSuggestionsDifficulty)[keyof typeof GetLearnSuggestionsDifficulty];

export const GetLearnSuggestionsDifficulty = {
  beginner: "beginner",
  intermediate: "intermediate",
  advanced: "advanced",
} as const;

export type GetLearnProgressParams = {
  userId: string;
};

export type GetLearnStreakParams = {
  /**
   * The user's unique identifier
   */
  userId: string;
};

export type GetLearnActivityParams = {
  /**
   * The user's unique identifier
   */
  userId: string;
  /**
   * How many weeks of history to return (default 26)
   * @minimum 1
   * @maximum 52
   */
  weeks?: number;
};
